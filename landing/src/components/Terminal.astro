<section class="section">
  <h2>Terminal Demo</h2>
  <div class="terminal card mono" aria-label="Codexa terminal demo">
    <p><span class="prompt">$</span> codexa check</p>
    <p class="line line1">Scanning staged files...</p>
    <p class="line line2">[MODERATE] src/auth.ts:32 no-unused-vars</p>
    <p class="line line3">Press <strong>[x]</strong> to apply fix</p>
    <p class="line line4">Fix applied. Re-running checks...</p>
    <p class="line line5">Clean. Commit allowed.</p>
  </div>
</section>

<style>
  h2 {
    margin-bottom: 12px;
  }

  .terminal {
    padding: 16px;
    min-height: 210px;
    overflow: hidden;
  }

  .terminal p {
    margin: 0 0 8px;
  }

  .prompt {
    color: var(--green);
    margin-right: 8px;
  }

  .line {
    opacity: 0;
    transform: translateY(4px);
    animation: reveal 6s infinite;
  }

  .line1 { animation-delay: 0.4s; }
  .line2 { animation-delay: 1.3s; color: #ffd27a; }
  .line3 { animation-delay: 2.2s; }
  .line4 { animation-delay: 3.1s; color: var(--orange); }
  .line5 { animation-delay: 4.1s; color: var(--green); }

  @keyframes reveal {
    0% { opacity: 0; transform: translateY(4px); }
    8% { opacity: 1; transform: translateY(0); }
    30% { opacity: 1; }
    40% { opacity: 0; }
    100% { opacity: 0; }
  }
</style>
